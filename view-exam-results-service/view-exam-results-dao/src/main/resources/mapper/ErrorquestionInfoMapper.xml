<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qgexam.exam.viewresults.dao.ErrorquestionInfoDao">
    <select id="selectErrorquestionInfoList" resultType="com.qgexam.exam.viewresults.pojo.PO.ErrorquestionInfo">
        select *
        from errorquestion_info ei, course_info ci
        where ei.student_id = #{studentId}
          and ci.course_id = #{courseId}
          and ei.subject_id = ci.subject_id
          and is_deleted = 0
    </select>
</mapper>