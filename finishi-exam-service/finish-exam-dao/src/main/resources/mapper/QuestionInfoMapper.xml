<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qgexam.exam.finish.dao.QuestionInfoDao">

    <select id="hasSubQuestion" resultType="java.lang.Integer">
        select has_sub_question from question_info where question_id=#{questionId} and is_delete=0
    </select>
    <select id="geyTypeByQuestionId" resultType="java.lang.String">
        select type from quetion_info where question_id=#{qustionId} and is_delete=0
    </select>
    <select id="getCorrectAnswer" resultType="java.lang.String">
        select question_ans from question_info where question_id=#{questionId} and is_delete=0
    </select>
</mapper>